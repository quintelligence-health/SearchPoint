

public class SearchPointJava {

	private static final SearchPointJava instance = new SearchPointJava();
	
	static {
		try {
			System.loadLibrary("SearchPointJava");
		} catch (UnsatisfiedLinkError e) {
			e.printStackTrace();
			System.exit(1);
		}
	}
	
	public static synchronized SearchPointJava getInstance() { return instance; };
	
	private SearchPointJava() {
		init();
	}
	
	private native void init();
	
	public native String processQuery(String query, String clustKey, int limit);
	
	public native String rankByPos(double x, double y, String queryId);
	
	public native String fetchKeywords(double x, double y, String queryId);
	
	public native void close();
}
